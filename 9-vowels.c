#include <stdio.h>

void main()
{
    for(int i=97;i<=122;i++)
    {
        if(i=='a'|| i=='e' || i=='i' || i=='o' || i=='u')
        {
            printf("\n print vowels : %c",i);
        }
    }
}